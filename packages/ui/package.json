{
  "name": "@turborepo-test/ui",
  "version": "0.0.0",
  "private": true,
  "sideEffects": false,
  "main": "./index.tsx",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "tsup ./index.jsx --format esm,cjs --external react",
    "dev": "tsup ./index.jsx --format esm,cjs --watch --external react",
    "lint": "eslint ./**/**/*.jsx --fix",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "test": "jest --coverage"
  },
  "license": "MIT",
  "devDependencies": {
    "react": "^18.2.0",
    "config": "*",
    "tsup": "^5.10.1"
  }
}
